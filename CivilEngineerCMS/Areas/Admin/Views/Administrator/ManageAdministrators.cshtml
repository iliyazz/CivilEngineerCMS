@using Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<SelectEmployeesForAdministratorFormModel>

@{
    ViewData["Title"] = "Manage Administrators";
}

<h2 class="text-center">@ViewData["Title"]</h2>
<hr/>
<div justify-content: center>
    <div style="display: inline">
        <div class="row" align="center">
            <form method="post">
                <h4 class="mt-2">Employees in project</h4>
                @if (!Model.Any())
                {
                    <h4 class="mt-2">No employees have been added to the project</h4>
                }
                else
                {
                    <table class="table table-hover">
                        <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">First name</th>
                            <th scope="col">Last name</th>
                            <th scope="col">Email</th>
                            <th scope="col">Job title</th>
                        </tr>
                        </thead>
                        <tbody>
                        @{
                            int rowNumber = 0;
                        }
                        @foreach (var e in Model)
                        {
                            rowNumber++;
                            <tr>
                                <th scope="row">@rowNumber</th>
                                <td>@e.FirstName</td>
                                <td>@e.LastName</td>
                                <td>@e.Email</td>
                                <td>@e.JobTitle</td>
                                <td>
                                    <input type="checkbox" asp-for=@e.IsChecked id=@e.UserId name="selectedEmployee" value=@e.UserId class="w3-check">
                                    <input type="hidden" asp-for=@e.UserId/>
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                }
                <div class="text-center">
                    <input class="btn btn-warning mt-3" type="submit" value="Save"/>
                    <a class="btn btn- btn-primary mt-3" href="javascript: history.back();">Cancel</a>

                </div>
            </form>
        </div>
    </div>
</div>